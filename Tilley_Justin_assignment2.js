//variables
var name = "Mike";
var jumpName = ["small", "big", "double", "triple"];
var jumpNumber = 1;
//procedure

//boolean


//array

var jump = function() {
	var feet = [5, 10, 15,]
	for (jumpHeight= 0; jumpHeight < feet.length; jumpHeight++) {
		console.log(name + " hits the " + jumpName[jumpNumber] + " jump and goes " + feet[jumpHeight] + " feet.")
		};
	var height = jumpNumber + feet
	return height;


};
jump()
//number

var gear = function(throttle) {
		var speed
		var jumpSpeed = jumpNumber * 2
		while(jumpSpeed > 0) { 
		console.log(name + " can make the jump!");	jumpSpeed--
		};
		speed = throttle * jumpNumber
		return speed
};
var bikeSpeed = gear(5)
console.log(name + " makes the jump at " + bikeSpeed + " mph!")

//string

var tire = function() {
	var thought = "You can do this " + name;
	
	return thought
};